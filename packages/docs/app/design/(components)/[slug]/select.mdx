import { Container, Select, Spacer } from "nextjs-components";
import { ArrowUp as Up } from "nextjs-components/src/icons";

<Spacer y={0.6} />

# Select

<Spacer y={0.8} />

## Display a dropdown list of items.

<Example style={{ "--width": "60%" }}>
  <Select placeholder="With label">
    <option>Option 1</option>
  </Select>
</Example>

<Spacer y={2} />

### Sizes

<Editor 
  scope={{Container,Select}}
  code={`
<Container direction={['column', 'row', 'row']} top>
  <Container left>
    <Select size="small" placeholder="Small" />
  </Container>

<Container left>
  <Select placeholder="Default" />
</Container>

  <Container left>
    <Select size="large" placeholder="Large" />
  </Container>
</Container>
`} />

<Spacer y={2} />

### Sizes, Prefix, and Suffix

<Editor 
  scope={{Container,Select,Up}}
  code={`
<Container direction={['column', 'row', 'row']} top>
  <Container left>
    <Select
      size="small"
      placeholder="Small"
      prefix={<Up />}
      suffix={<Up />}
    />
  </Container>

<Container left>
  <Select placeholder="Default" prefix={<Up />} suffix={<Up />} />
</Container>

  <Container left>
    <Select
      size="large"
      placeholder="Large"
      prefix={<Up />}
      suffix={<Up />}
    />
  </Container>
</Container>
`} />

<Spacer y={2} />

### Disabled

<Editor
  scope={{ Container, Select }}
  code={`
<Container direction={['column', 'row', 'row']} top>
  <Container>
    <Select placeholder="Disabled with placeholder" disabled />
  </Container>
</Container>
`}
/>

<Spacer y={2} />

### Prefix and Suffix Disabled

<Editor
  scope={{ Container, Select, Up }}
  code={`
<Container left>
  <Container>
    <Select placeholder="Default" prefix={<Up />} disabled />
  </Container>
</Container>
`}
/>

<Spacer y={2} />

### Other

<Editor
  scope={{ Container, Select }}
  code={`
<Container>
  <Select placeholder="With label" label="My label" />
</Container>
`}
/>

<Spacer y={4} />
