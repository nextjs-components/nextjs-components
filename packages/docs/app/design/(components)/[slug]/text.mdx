import { Code, InlineCode, LI, OL, Stack, Text, UL } from "nextjs-components";

<Spacer y={0.6} />

# Text

<Spacer y={0.8} />

## Display text using well-defined typographic styles.

<Example style={{ "--width": "auto" }}>
  <Stack gap={2}>
    {[48, 32, 24, 20, 16, 14, 12, 10].map((size) => (
      <Text key={size} size={size}>
        The Happy Bufo laughs.
      </Text>
    ))}
  </Stack>
</Example>

### Props

| Name                    | Type                                                          | Description                                                                                                                      | Default            |
| ----------------------- | ------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------- | ------------------ |
| children                | `ReactNode`                                                   | The content, duh.                                                                                                                | -                  |
| as                      | `enum`                                                        | The root element.                                                                                                                | `p`                |
| size                    | `10 \| 12 \| 14 \| 16 \| 20 \| 24 \| 32 \| 40 \| 48`          | The font size.                                                                                                                   | `14`               |
| lineHeight              | `12 \| 16 \| 20 \| 24 \| 32 \| 40 \| 48 \| 56`                | The line height.                                                                                                                 | -                  |
| weight                  | `100 \| 200 \| 300 \| 400 \| 500 \| 600 \| 700 \| 800 \| 900` | The font weight.                                                                                                                 | -                  |
| transform               | `"capitalize" \| "uppercase" \| "lowercase" \| "initial"`     | Text transform short hand.                                                                                                       | -                  |
| align                   | `"left" \| "center" \| "right"`                               | Text alignment short hand.                                                                                                       | -                  |
| color                   | `enum`                                                        | The text color. Strictly limited to colors of the ∆ design system.                                                               | `geist-foreground` |
| truncate                | `number \| boolean`                                           | Truncate a single or multiple line(s). If you pass truncate, make sure to pass `title` so that the full value is shown on hover. | -                  |
| className               | `string`                                                      | The root class name.                                                                                                             | -                  |
| style                   | `CSSProperties`                                               | The root element inline stlyes.                                                                                                  | -                  |
| title                   | `string`                                                      | If text is \`truncated\`, this should be the full text.                                                                          | -                  |
| wrap                    | `boolean`                                                     | Whether the text should wrap lines                                                                                               | `true`             |
| dangerouslySetInnerHTML | `{__html: string}`                                            | See https://reactjs.org/docs/dom-elements.html#dangerouslysetinnerhtml                                                           | -                  |

<Spacer y={2} />

### Sizes

<Editor
  scope={{ Stack, Text }}
  code={`
<Stack gap={2}>
  <Text size={48}>
    The Happy Bufo laughs.
  </Text>
  <Text size={32}>
    The Happy Bufo laughs.
  </Text>
  <Text size={24}>
    The Happy Bufo laughs.
  </Text>
  <Text size={20}>
    The Happy Bufo laughs.
  </Text>
  <Text size={16}>
    The Happy Bufo laughs.
  </Text>
  <Text size={14}>
    The Happy Bufo laughs.
  </Text>
  <Text size={12}>
    The Happy Bufo laughs.
  </Text>
  <Text size={10}>
    The Happy Bufo laughs.
  </Text>
</Stack>
`}
/>

<Spacer y={2} />

### Weights

<Editor
  scope={{ Stack, Text }}
  code={`
<Stack gap={2}>
  {[900, 800, 700, 600, 500, 400, 300, 200, 100].map(weight => (
    <Text key={weight} size={16} weight={weight}>
      The Happy Bufo laughs. ({weight})
    </Text>
  ))}
</Stack>
`}
/>

<Spacer y={2} />

### Color

<Editor
  scope={{ Text }}
  code={`
  <Text size={16} color="geist-success">
  The Happy Bufo laughs.
</Text>
`}
/>

<Spacer y={2} />

### Modifiers

<Editor
  scope={{ Text }}
  code={`
<Text size={16}>
  The <strong>Happy Bufo</strong> <em>laughs</em> at the <s>silly bunny</s> <u>Funny Meme</u>.
</Text>
`}
/>

<Spacer y={2} />

### Polymorphic

<Editor
  scope={{ Text }}
  code={`
<Text as="p" size={32}>
  {\`<p>\`} semantically, {\`<h2>\`} visually.
</Text>
`}
/>

<Spacer y={2} />

### Truncate

<Editor
  scope={{ Text }}
  code={`
<div style={{ maxWidth: 100 }}>
  <Text size={16} truncate>The Happy Bufo laughs.</Text>
</div>
`}
/>

<Spacer y={2} />

### Clamp

<Editor
  scope={{ Text }}
  code={`
<div style={{ maxHeight: 100 }}>
  <Text size={16} truncate={2}>
    The Happy Bufo laughs. The Happy Bufo laughs. The Happy Bufo laughs.
    The Happy Bufo laughs. The Happy Bufo laughs. The Happy Bufo laughs.
    The Happy Bufo laughs. The Happy Bufo laughs. The Happy Bufo laughs.
    The Happy Bufo laughs. The Happy Bufo laughs.
  </Text>
</div>
`}
/>

<Spacer y={2} />

### Align

<Editor
  scope={{ Text }}
  code={`
<div>
  <Text align="center">
    The Happy Bufo laughs.
  </Text>

<Text align="left">The Happy Bufo laughs.</Text>

  <Text align="right">
    The Happy Bufo laughs.
  </Text>
</div>
`}
/>

<Spacer y={2} />

### Monospace Font

<Editor
  scope={{ Text }}
  code={`
<div>
  <Text>
    This is a sans-serif font.
  </Text>

  <Text monospace>
    This is a monospace font.
  </Text>
</div>
`}
/>

<Spacer y={2} />

### Unordered List

<Editor
  scope={{ UL, LI }}
  code={`
<UL>
  <LI>Static websites and static generators (React, Vue, Angular, etc)</LI>
  <LI>Code that renders HTML on the server-side</LI>
  <LI>API endpoints that query databases or web APIs and return dynamic data</LI>
</UL>
`}
/>

<Spacer y={2} />

### Ordered List

<Editor
  scope={{ OL, LI }}
  code={`
<OL>
  <LI>
    Delete all files, commit, and push to your GitHub repository.
  </LI>
  <LI>
    Can’t have any buggy code if you don’t have any code.
  </LI>
  <LI>
    Profit.
  </LI>
</OL>
`}
/>

<Spacer y={2} />

### Code

<Editor
  scope={{ Text, InlineCode }}
  code={`
<Text size={16}>
  To use these nice components, run <InlineCode>npm i nextjs-components</InlineCode>.
</Text>
`}
/>

<Spacer y={2} />

### Codeblock

<Editor
  scope={{ Code }}
  code={`
<Code>{\`npm i nextjs-components\nnpm ls nextjs-components\`}</Code>
`}
/>

<Spacer y={4} />
