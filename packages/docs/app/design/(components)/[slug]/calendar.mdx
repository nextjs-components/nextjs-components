import { parseDate } from "@internationalized/date";
import { Calendar } from "nextjs-components";

export const presets = {
  "last-3-days": {
    start: new Date(Date.now() - 3 * 24 * 60 * 60 * 1000),
    text: "Last 3 days",
  },
  "last-7-days": {
    start: new Date(Date.now() - 7 * 24 * 60 * 60 * 1000),
    text: "Last 7 days",
  },
  "last-14-days": {
    start: new Date(Date.now() - 14 * 24 * 60 * 60 * 1000),
    text: "Last 14 days",
  },
  "last-month": {
    start: new Date(Date.now() - 30 * 24 * 60 * 60 * 1000),
    text: "Last month",
  },
};

<Spacer y={0.6} />

# Calendar

<Spacer y={0.8} />

Displays a calendar from which users can select a date or range of dates.

<Spacer />

### Default

<Editor
  scope={{ Calendar, presets, parseDate }}
  code={`() => {
  const [range, setRange] = React.useState(null)
  
  return <Calendar 
    value={range}
    onChange={setRange}
    // minValue={parseDate("2023-02-07")}
    // maxValue={parseDate("2023-05-20")}
  />
}`}
/>

<Spacer y={4} />
