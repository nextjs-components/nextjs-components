---
"nextjs-components": major
---

Require Tailwind CSS 4 or later as a peer dependency. Add a shared Tailwind build and theme for the component library, and migrate Calendar layout utilities to it. Ship compiled, prefixed CSS through the existing global stylesheet. Align shared gray and blue tokens and the font stack with Geist, removing Calendar-specific theme overrides.
